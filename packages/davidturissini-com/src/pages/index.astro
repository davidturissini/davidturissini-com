---
import { getCollection } from 'astro:content';
import { Image } from 'astro:assets';
import Base from '../layouts/Base.astro';
import Avatar from '../images/avatar.jpg';
const posts = await getCollection('blog');
---

<Base title="David Turissini's Blog">
	<div class="bg-gray-100">
    <!-- Hero Section -->
    <section class="bg-white py-12">
      <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 text-center">
				<div class="overflow-hidden h-auto block w-32 h-32 rounded-full mx-auto">
					<Image
						src={Avatar}
						alt="David Turissini profile picture"
					/>
				</div>
        <h1 class="font-normal text-ls font-bold mt-4">
          David Turissini - Principal Engineer at Hungryroot. 
        </h1>
        <p class="mt-4 text-lg text-gray-600">
          I help teams ship in hours, not weeks, through engineering excellence, continuous delivery practices, and a <span class="whitespace-nowrap">'ship-anytime' mentality.</span> <span class="whitespace-nowrap">Ex-Apple & Salesforce.</span>
        </p>
      </div>
    </section>

    <!-- Latest Posts Section -->
    <section class="bg-gray-50 py-12">
      <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <h2 class="text-3xl font-bold mb-8 text-center">Latest Posts</h2>
        <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8">
          {posts.filter((post) => post.data.draft !== true).map(post => (
            <article class="bg-white shadow-lg rounded-lg overflow-hidden">
              <div class="p-6">
                <h3 class="text-xl font-bold mb-2">
                  <a href={`/blog/${post.slug}`} class="text-blue-600 hover:underline">
                    {post.data.title}
                  </a>
                </h3>
              </div>
            </article>
          ))}
        </div>
      </div>
    </section>
  </div>
</Base>
